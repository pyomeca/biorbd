# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: biorbd_casadi
channels:
- conda-forge
- default
dependencies:
- python >=3.10
- numpy
- matplotlib
- casadi
- rbdl=*=*casadi*
- scipy
- ezc3d
